server:
  port: 8082

spring:
  application:
    name: its-learning-management

  data:
    mongodb:
      uri: mongodb://foo:bar@103.140.249.159:27017/its?authSource=admin

  # Import shared JWT configuration from its-common module
  config:
    import: classpath:application-jwt.yml

  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 50MB

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dsvyour9p}
  api-key: ${CLOUDINARY_API_KEY:987671397958468}
  api-secret: ${CLOUDINARY_API_SECRET:A25C6xZpykPCtrhwv_r3hKS7Ha4}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}

management:
  endpoints:
    web:
      exposure:
        include: health,info
